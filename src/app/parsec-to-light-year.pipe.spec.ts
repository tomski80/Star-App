import { ParsecToLightYearPipe } from './parsec-to-light-year.pipe';

describe('ParsecToLightYearPipe', () => {
  it('create an instance', () => {
    const pipe = new ParsecToLightYearPipe();
    expect(pipe).toBeTruthy();
  });
});

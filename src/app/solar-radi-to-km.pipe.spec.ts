import { RadiiToKmPipe } from './solar-radi-to-km.pipe';

describe('RadiiToKmPipe', () => {
  it('create an instance', () => {
    const pipe = new RadiiToKmPipe();
    expect(pipe).toBeTruthy();
  });
});
